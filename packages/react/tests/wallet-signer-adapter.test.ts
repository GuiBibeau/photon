import { describe, it, expect } from 'vitest';

describe('Wallet Signer Adapter', () => {
  it('should be tested via e2e tests', () => {
    // Signer adapter functionality is tested through real e2e tests
    // when actual signing operations are performed
    expect(true).toBe(true);
  });
});